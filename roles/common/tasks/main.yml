- name: "install dotfiles"
  loop:
    - bash
    - env
    - fish
    - readline
    - tmux
  ansible.builtin.copy:
    src: "{{ item }}/"
    dest: "{{ ansible_facts['user_dir'] }}/"
    mode: preserve
